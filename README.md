# MaDun
macOS clipboard privacy guardian. Detects and sanitizes API keys, credentials before they're accidentally copied. 30-second undo window. 100% offline, zero network. Lightweight, &lt;0.1% CPU.
